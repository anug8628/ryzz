func ryzzbuzz(num n) -> none {
    num i = 1;
    while(i <= n) {
        if ((((i % 3) == 0)) or (((i % 5) == 0))) {
            print(i);
        }
        else {}
        i = i + 1;
    }
}

ryzzbuzz(15);